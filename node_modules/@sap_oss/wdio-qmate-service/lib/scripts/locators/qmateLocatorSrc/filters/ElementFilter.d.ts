import { BaseFilter } from "./BaseFilter";
export declare class ElementFilter extends BaseFilter {
    protected doFiltering(controls: UI5Control[]): UI5Control[];
    doCheckSingle(control: UI5Control): boolean;
    private checkElementProperties;
    static filterBySelector(ui5Selector: UI5Selector, controls: UI5Control[]): UI5Control[];
}
