import { BaseFilter } from "../filters/BaseFilter";
export declare class FilterFactory {
    private instances;
    getInstance<T extends BaseFilter>(classType: new (...args: any[]) => T, elementProperties: ElementProperties | undefined): T;
}
