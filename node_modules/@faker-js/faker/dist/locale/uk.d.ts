import { F as Faker } from '../airline-DF6RqYmq.js';

/**
 * The faker instance for the `uk` locale.
 *
 * - Language: Ukrainian
 * - Endonym: Українська
 *
 * This instance uses the following locales internally (in descending precedence):
 *
 * - `uk`
 * - `en`
 * - `base`
 */
declare const faker: Faker;

export { faker };
