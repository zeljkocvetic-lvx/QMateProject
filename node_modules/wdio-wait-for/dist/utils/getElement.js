export const getElement = async (selectorOrElement) => {
    return typeof selectorOrElement === 'string'
        ? await $(selectorOrElement).getElement()
        : await selectorOrElement.getElement();
};
